name: Go

on:
  push:
    branches: [ "default" ]
  pull_request:
    branches: [ "default" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.27'

    - name: Test
      run: go test -v ./...
